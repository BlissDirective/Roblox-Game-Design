--!strict
--[[
	Outpost-7 — first-party asset ID manifest (single source of truth).

	AUTO-GENERATED by tools/scripts/regen-asset-ids.py from
	tools/asset-import/upload-manifest.json. Do NOT hand-edit ids here — add a
	row to the manifest (or run tools/harness/seed_upload_rows.py) and re-run
	the generator / the upload workflow.

	Every `rbxassetid://` the game loads for a first-party asset lives here —
	never a literal `rbxassetid://…` in a module (same rule as "no magic
	numbers", per CLAUDE.md). `rbxassetid://0` means "not uploaded yet";
	consumers treat it as a no-op via AssetIds.isPlaceholder.

	Categories:
	  Icons / Thumbs / Images — 2D listing, store, HUD art (Decal uploads)
	  Music / Sfx / Ambient   — audio (Audio uploads)
	  Skyboxes                — six faces per biome: <Biome><Ft|Bk|Lf|Rt|Up|Dn>
	  Textures                — particle / beam / decal / floor textures (Decal)
	  Models                  — FBX uploads (Model); loaded at boot by
	                            Server.World.ArtTemplateLoader into
	                            ServerStorage.ArtTemplates and attached to the
	                            procedural placeholders by Shared.Lib.VisualAttach
--]]

export type AssetManifest = { [string]: { [string]: string } }

local PLACEHOLDER = "rbxassetid://0"

local AssetIds = {
	Icons = {
		DecalChevronRed = PLACEHOLDER,
		DecalOp7Emblem = PLACEHOLDER,
		DecalSkullAmber = PLACEHOLDER,
		FlairOp7Veteran = PLACEHOLDER,
		FlairPioneer = PLACEHOLDER,
		FtuePointerArrow = PLACEHOLDER,
		GameIcon = PLACEHOLDER,
		StoreDpCores = PLACEHOLDER,
		StoreDpCreditsL = PLACEHOLDER,
		StoreDpCreditsS = PLACEHOLDER,
		StoreDpShield = PLACEHOLDER,
		StorePass2xCredits = PLACEHOLDER,
		StorePassAutoCollect = PLACEHOLDER,
		StorePassBpPremium = PLACEHOLDER,
		StorePassVip = PLACEHOLDER,
		TrailNeonBlue = PLACEHOLDER,
		TrailNeonGold = PLACEHOLDER,
		TrailNeonRed = PLACEHOLDER,
		UiBattlePass = PLACEHOLDER,
		UiBuild = PLACEHOLDER,
		UiClan = PLACEHOLDER,
		UiCombat = PLACEHOLDER,
		UiCores = PLACEHOLDER,
		UiCosmetics = PLACEHOLDER,
		UiCredits = PLACEHOLDER,
		UiFriends = PLACEHOLDER,
		UiLeaderboard = PLACEHOLDER,
		UiQuests = PLACEHOLDER,
		UiRepair = PLACEHOLDER,
		UiShield = PLACEHOLDER,
		UiShop = PLACEHOLDER,
		UiStore = PLACEHOLDER,
		UiVoice = PLACEHOLDER,
		UiWaveAlarm = PLACEHOLDER,
	},
	Thumbs = {
		Defense = PLACEHOLDER,
		Raid = PLACEHOLDER,
		Transformation = PLACEHOLDER,
	},
	Images = {
		FtueIntroFrame = PLACEHOLDER,
		StoreBpBanner = PLACEHOLDER,
	},
	Music = {
		Build = PLACEHOLDER,
		Combat = PLACEHOLDER,
		Defeat = PLACEHOLDER,
		Raid = PLACEHOLDER,
		Victory = PLACEHOLDER,
	},
	Sfx = {
		AlienGrowl = PLACEHOLDER,
		ClaimChime = PLACEHOLDER,
		DroneFire = PLACEHOLDER,
		ExtractorPlace = PLACEHOLDER,
		FtueIntroSting = PLACEHOLDER,
		FtueStepAdvance = PLACEHOLDER,
		RepairComplete = PLACEHOLDER,
		StructureBreach = PLACEHOLDER,
		TurretFire = PLACEHOLDER,
		UiClick = PLACEHOLDER,
		UiPanelOpen = PLACEHOLDER,
		WallPlace = PLACEHOLDER,
		WaveAlarm = PLACEHOLDER,
		WeaponFire = PLACEHOLDER,
	},
	Ambient = {
		Ice = PLACEHOLDER,
		Jungle = PLACEHOLDER,
		Volcanic = PLACEHOLDER,
	},
	Skyboxes = {
		IceBk = PLACEHOLDER,
		IceDn = PLACEHOLDER,
		IceFt = PLACEHOLDER,
		IceLf = PLACEHOLDER,
		IceRt = PLACEHOLDER,
		IceUp = PLACEHOLDER,
		JungleBk = PLACEHOLDER,
		JungleDn = PLACEHOLDER,
		JungleFt = PLACEHOLDER,
		JungleLf = PLACEHOLDER,
		JungleRt = PLACEHOLDER,
		JungleUp = PLACEHOLDER,
		VolcanicBk = PLACEHOLDER,
		VolcanicDn = PLACEHOLDER,
		VolcanicFt = PLACEHOLDER,
		VolcanicLf = PLACEHOLDER,
		VolcanicRt = PLACEHOLDER,
		VolcanicUp = PLACEHOLDER,
	},
	Textures = {
		DecalChevronRed = PLACEHOLDER,
		DecalOp7Emblem = PLACEHOLDER,
		DecalSkullAmber = PLACEHOLDER,
		DmgExtractorDisabled = PLACEHOLDER,
		DmgTurretDamaged = PLACEHOLDER,
		DmgWallBreached = PLACEHOLDER,
		DmgWallCracked = PLACEHOLDER,
		PropPlotFloor = PLACEHOLDER,
		RaidArenaFloor = PLACEHOLDER,
		TrailNeonBlue = PLACEHOLDER,
		TrailNeonGold = PLACEHOLDER,
		TrailNeonRed = PLACEHOLDER,
		VfxAshVolcanic = PLACEHOLDER,
		VfxBeamDrone = PLACEHOLDER,
		VfxBeamTurret = PLACEHOLDER,
		VfxFrostIce = PLACEHOLDER,
		VfxImpactSpark = PLACEHOLDER,
		VfxSporeJungle = PLACEHOLDER,
		WeaponMuzzleFlash = PLACEHOLDER,
	},
	Models = {
		AlienCryowraith = PLACEHOLDER,
		AlienMagmaling = PLACEHOLDER,
		AlienStalker = PLACEHOLDER,
		ArchIce = PLACEHOLDER,
		ArchJungle = PLACEHOLDER,
		ArchVolcanic = PLACEHOLDER,
		BuildExtractor = PLACEHOLDER,
		BuildTurretAuto = PLACEHOLDER,
		BuildTurretHeavy = PLACEHOLDER,
		BuildTurretLaser = PLACEHOLDER,
		BuildWall = PLACEHOLDER,
		CaveGlacierWall = PLACEHOLDER,
		CaveStalactite = PLACEHOLDER,
		CaveStalagmite = PLACEHOLDER,
		DroneCombat = PLACEHOLDER,
		DroneEngineering = PLACEHOLDER,
		DroneRecon = PLACEHOLDER,
		FloraIceCrystal = PLACEHOLDER,
		FloraIceFrozenShrub = PLACEHOLDER,
		FloraJungleGlowFern = PLACEHOLDER,
		FloraJungleSporePod = PLACEHOLDER,
		FloraJungleVineTree = PLACEHOLDER,
		FloraVolcanicAshTree = PLACEHOLDER,
		FloraVolcanicFissure = PLACEHOLDER,
		FtueRingBeacon = PLACEHOLDER,
		PropDropshipPad = PLACEHOLDER,
		PropPlotBorder = PLACEHOLDER,
		PropResourceNode = PLACEHOLDER,
		RaidAttackerPad = PLACEHOLDER,
		RaidLootCore = PLACEHOLDER,
		SkinCombatant = PLACEHOLDER,
		SkinHeavyDefender = PLACEHOLDER,
		SkinRecon = PLACEHOLDER,
		VehicleHoverBike = PLACEHOLDER,
		VehicleHoverTank = PLACEHOLDER,
		VehicleRover = PLACEHOLDER,
		WeaponRifleView = PLACEHOLDER,
		WeaponRifleWorld = PLACEHOLDER,
	},
}

-- True when an id is still the unset placeholder. Consumers (AudioController,
-- BiomeLightingService, ArtTemplateLoader, HUD) use this to no-op cleanly on
-- a dev build instead of spawning a broken Sound / Sky / Image / Model.
function AssetIds.isPlaceholder(id: string?): boolean
	return id == nil or id == PLACEHOLDER or id == "rbxassetid://0" or id == "0"
end

return AssetIds
-- required-check surface: no behavior change
