--!strict
-- Outpost-7 — shared type definitions.
--
-- Phase A1 scaffold: empty. Phase A3 adds PlayerProfile, BaseLayout, Quest,
-- Blueprint, Buildable, etc. Consumers `require` this module purely for the
-- `export type` declarations; the returned table stays empty by convention.

return {}
