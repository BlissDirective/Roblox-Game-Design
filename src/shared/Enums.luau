--!strict
-- Outpost-7 — string enum tables (Mode, Biome, ItemRarity, etc.).
--
-- Phase A1 scaffold: empty. Populated by later phases as their systems require
-- shared enum identity (e.g., Mode = { Build = "Build", Combat = "Combat" }).

local Enums = {}

return Enums
